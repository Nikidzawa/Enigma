spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/enigma
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  mvc:
    servlet:
      path: /api/
server:
  port: 9000